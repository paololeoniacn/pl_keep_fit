# Build artifact per il deploy su Cloudflare Pages (rigenerato da deploy.sh)
dist/

# Wrangler
.wrangler/
node_modules/

# Credenziali deploy (segreti — mai committare)
.dev.vars
.env

# macOS
.DS_Store

# Permessi personali Claude Code (locali, non condivisi)
.claude/settings.local.json

# Materiale sorgente pesante (reel Instagram) — ri-scaricabile, non versionato
NEWS/*.mp4
